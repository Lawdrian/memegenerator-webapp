import {Grid} from "@mui/material";

function ImageEditorContainer({children}) {

    return(
        <Grid container>
            {children}
        </Grid>
    )
}

export default ImageEditorContainer;